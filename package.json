{
  "name": "ganhammar",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build && npm run build:lambda && npm run build:package",
    "build:lambda": "esbuild lambda/handler.ts --bundle --platform=node --target=node20 --format=esm --outfile=build/index.mjs --external:./server/* --external:./client/*",
    "build:package": "echo '{\"type\":\"module\"}' > build/package.json",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "cdk": "cdk",
    "deploy": "cdk deploy --require-approval never --app 'npx tsx infra/bin/app.ts'"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1",
    "shiki": "^3.21.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.5.1",
    "@sveltejs/kit": "^2.49.5",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.9",
    "aws-cdk": "^2.1101.0",
    "aws-cdk-lib": "^2.235.0",
    "constructs": "^10.4.5",
    "esbuild": "^0.27.2",
    "svelte": "^5.46.4",
    "svelte-check": "^4.3.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
